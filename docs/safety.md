# Monitoring safety model

Mandatory source, category and keyword wake-ups execute before the classifier. Seen suppression is content-addressed. Reconciliation compares every fetched content hash with evaluated history. An empty digest is positive evidence the job ran; delivery failures are returned, never converted into a quiet success.
